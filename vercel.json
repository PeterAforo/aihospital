{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd backend && npx prisma generate && cd ../api && npx prisma generate && cd ../frontend && pnpm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "pnpm install --recursive",
  "framework": "vite",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "backend/prisma/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
