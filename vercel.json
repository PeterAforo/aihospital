{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd backend && pnpm install && pnpm exec prisma generate && cd ../api && pnpm install && pnpm exec prisma generate && cd ../frontend && pnpm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "backend/prisma/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
