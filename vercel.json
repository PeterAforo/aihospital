{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd backend && npx prisma@5.22.0 generate && cd ../api && npx prisma@5.22.0 generate && cd ../frontend && npx vite@5.1.0 build",
  "outputDirectory": "frontend/dist",
  "installCommand": "pnpm install --recursive",
  "framework": "vite",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "backend/prisma/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
